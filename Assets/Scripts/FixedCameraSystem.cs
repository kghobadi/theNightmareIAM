using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FixedCameraSystem : MonoBehaviour {
    // while player walks around each scene, probably want some slight camera movement
    //the trigger script can set new positions and rotations for the camera initially
    //but this script can have a range at which camera moves/rotates to provide clarity
    
	void Start () {
		
	}
	
	void Update () {
		
	}
}
